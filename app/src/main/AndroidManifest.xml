<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pop.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".Customerregistration"></activity>
        <activity android:name=".elephantownerregistration" />
        <activity android:name=".accessoryownerregistration" />
        <activity android:name=".fansassociationregistration" />
        <activity android:name=".Viewelephantdetails" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Viewelepdetail">

        </activity>
    </application>

</manifest>